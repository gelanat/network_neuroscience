#Importing the necessary libraries
import numpy as np
import matplotlib.pyplot as plt
from nilearn import image, plotting, datasets
import nibabel as nib

#Loading the data using nibabel
data = nib.load('sub-01_task1_bold.nii.gz')
first_data = image.index_img(data1, 0)

#Using Nilearn's build-in function to get Echo-planar imaging plot (supposed to minimize patient's motion)
plotting.plot_epi(image.mean_img(first_data1))
plotting.show()
